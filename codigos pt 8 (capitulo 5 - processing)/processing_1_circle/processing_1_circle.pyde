def setup(): #define a altura e largura da janela q sera desenhado em pixels
    size(400,400)
def draw(): #função que desenha
    ellipse(200,100,20,20)
    #ellipse(x,y,width, height)
    #x e y = onde o centro desenho/ ellipse estara localizada no plano (cordenadas)
    #width e height => altura e largura do desenho
